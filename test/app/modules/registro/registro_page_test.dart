import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('RegistroPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RegistroPage(title: 'Registro')));
    //  final titleFinder = find.text('Registro');
    //  expect(titleFinder, findsOneWidget);
  });
}
