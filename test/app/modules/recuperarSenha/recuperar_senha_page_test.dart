import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('RecuperarSenhaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RecuperarSenhaPage(title: 'RecuperarSenha')));
    //  final titleFinder = find.text('RecuperarSenha');
    //  expect(titleFinder, findsOneWidget);
  });
}
