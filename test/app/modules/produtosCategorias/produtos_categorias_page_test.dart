import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ProdutosCategoriasPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ProdutosCategoriasPage(title: 'ProdutosCategorias')));
    //  final titleFinder = find.text('ProdutosCategorias');
    //  expect(titleFinder, findsOneWidget);
  });
}
