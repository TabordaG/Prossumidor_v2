import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ProdutoDetalhesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ProdutoDetalhesPage(title: 'ProdutoDetalhes')));
    //  final titleFinder = find.text('ProdutoDetalhes');
    //  expect(titleFinder, findsOneWidget);
  });
}
